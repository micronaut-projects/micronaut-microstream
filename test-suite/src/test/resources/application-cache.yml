microstream:
  storage:
    backing: # <1>
      storage-directory: "${storageDirectory}"
      channel-count: 4
  cache:
    counter:
      key-type: java.lang.String
      value-type: java.lang.Long
      backing-storage: backing # <2>
spec:
  name: CacheTest
